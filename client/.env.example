## Frontend environment variables (Vite)
#
# Dev proxy target for local API (HTTPS by default).
# The browser will still call /api/* on the Vite dev server,
# and Vite will proxy it to this target (including self-signed certs).
#
# Vite only exposes variables prefixed with VITE_ to the browser.

VITE_API_PROXY_TARGET=https://localhost:4001

